{
  "name": "hh-server-cloudflare",
  "private": true,
  "version": "1.0.0",
  "description": "Deploy Café Hồng Hoa lên Cloudflare Workers (thay cho Render) — xem README.md trong thư mục này.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  },
  "dependencies": {
    "express": "^5.2.1",
    "cookie-parser": "^1.4.7",
    "pg": "^8.16.3"
  }
}
